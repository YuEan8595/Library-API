# Copy to .env and adjust. Nothing here is baked into the image (12-Factor III).
POSTGRES_DB=library
POSTGRES_USER=library
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

API_PORT=8080
LOG_LEVEL_APP=DEBUG

# OAuth2 (see README "Authentication & authorization" / ASSUMPTIONS.md 4.1)
OAUTH2_ISSUER_URI=http://localhost:8080
LIBRARY_STAFF_CLIENT_SECRET=change-me
